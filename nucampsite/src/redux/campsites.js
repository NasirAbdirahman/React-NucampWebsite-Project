import { CAMPSITES } from '../shared/campsites';

//Reducer Function

export const Campsites = (state = CAMPSITES, action) => {
    switch (action.type) {
        default:
            return state;
    }
};